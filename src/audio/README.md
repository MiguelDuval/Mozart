# Audio Layer

This directory is reserved for the optional audio-preview subsystem.

Planned foundations:

- Oboe for Android low-latency audio;
- JUCE utilities where they provide clear value;
- Tracktion Engine only when the higher-level sequencing/audio model is needed.

Audio must never become a prerequisite for MIDI timing.
