# Scheduler Layer

This directory will contain the bounded look-ahead MIDI scheduler.

The scheduler consumes semantic musical events plus clock information and produces timestamped transport events.

It must not know about Android UI classes.
